version: '3.8'

services:
  app:
    container_name: namoland-app
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    network_mode: host
    env_file:
      - .env
    volumes:
      - ./uploads:/app/public/uploads
